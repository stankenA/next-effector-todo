import { FC } from "react";
import styles from "./DateInfo.module.scss";
import CommonCard from "../CommonCard/CommonCard";

const DateInfo: FC = () => {
  return <CommonCard>bruh</CommonCard>;
};

export default DateInfo;
